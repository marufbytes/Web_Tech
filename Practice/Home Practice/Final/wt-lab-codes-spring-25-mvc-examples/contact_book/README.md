Contact Book MVC Example

- Import `sql/schema.sql` to create `contact_db` and sample contacts.
- Open `index.php` in your browser.

This shows a minimal contact list and add flow using the same MVC structure.
