Simple Todo MVC Example

- Import `sql/schema.sql` to create `todo_db` and sample rows.
- Open `index.php` in your browser (e.g. `http://localhost/wt_o/mvc-examples/simple_todo/index.php`).

This example demonstrates a minimal MVC flow: controller -> model -> view.
